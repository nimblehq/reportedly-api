default: &default
  DB_NAME: "reportedly_api"
  DB_HOST: "localhost"
  DB_PORT: "5432"
  DB_USERNAME: "postgres"
  MAILER_DEFAULT_HOST: "localhost"
  MAILER_DEFAULT_PORT: "3000"
  MAILER_SENDER: "Test <noreply@nimblehq.co>"
  AVAILABLE_LOCALES: "en"
  DEFAULT_LOCALE: "en"
  FALLBACK_LOCALES: "en"
  DEVISE_JWT_SECRET_KEY: "6337b328cf7f1fa2a9e6be9d05146367b746443e3bf317e00e1993294aaf59efd4fc030e8862231ecb931ff8588510b8e06df0098fc55c8f8b9676556d467ba5"

development:
  <<: *default

test:
  <<: *default
  TEST_RETRY: "0"

# Set environment variables required in the initializers in order to precompile the assets.
# Because it initializes the app, so all variables need to exist in the Docker build stage (used in bin/docker-assets-precompile).
docker_build:
  <<: *default
  SECRET_KEY_BASE:
